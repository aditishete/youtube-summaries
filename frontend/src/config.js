// Max videos shown per channel in the "All Channels" view
export const MAX_VIDEOS_PER_CHANNEL = 3;

// Max videos retained per channel in the database (also the single-channel display cap)
export const MAX_RETAINED_VIDEOS_PER_CHANNEL = 30;
